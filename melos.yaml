name: needle_orm

repository: https://github.com/needle-dart/needle_orm_all.dart

packages:
  - packages/**

command:
  bootstrap:
    usePubspecOverrides: true
    
scripts:
  analyze:
    exec: dart analyze .
  format:
    exec: dart format .
  doc:
    exec: dart doc
  doc-clean:
    exec: rm -rf doc

